<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="12dp"
    android:background="#f3f3f3">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_menu_select"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tv_menu"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@mipmap/un_select" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_menu"
        android:layout_marginLeft="12dp"
        app:layout_constraintStart_toEndOf="@id/iv_menu_select"
        app:layout_constraintTop_toTopOf="@id/iv_menu_select"
        app:layout_constraintBottom_toBottomOf="@id/iv_menu_select"
        app:layout_constraintEnd_toStartOf="@+id/tv_num"
        app:layout_constraintHorizontal_bias="0"
        android:text="流浪军"
        android:textSize="14sp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_num"
        app:layout_constraintStart_toEndOf="@id/tv_menu"
        app:layout_constraintTop_toTopOf="@id/iv_menu_select"
        app:layout_constraintBottom_toBottomOf="@id/iv_menu_select"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="15 条"
        android:textSize="14sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>